- Comment the code, especially integer computations.

- Add gzip support.

- Add populate.pl to the distribution.  For that server_versions
  command should be made public.

- Rename bins to buckets.
